# cloud-projects-templates

Multi-provider cloud dev environments (AWS, Azure, GCP, OCI, Kubernetes) built on
[devenv](https://devenv.sh), with Nix flake templates, `AGENTS.md` and agent skills.

Work in progress — see `intent/`, `spec/` and `plan/`.
